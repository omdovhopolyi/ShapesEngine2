#pragma once

#include "Texture.h"

#include <memory>
#include <glm/glm.hpp>

namespace shen
{
    /*class Sprite
    {
    public:
        const Texture* GetTexture() const;
        glm::vec2 GetOrigin() const;
        glm::vec2 GetSize() const;
        glm::vec2 GetLB() const;
        glm::vec2 GetRT() const;

    private:
        const Texture* _texture = nullptr;
        glm::vec2 _origin;
        glm::vec2 _size;
    };*/
}
