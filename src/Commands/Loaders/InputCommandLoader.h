#pragma once

#include "InputCommandsLoadersCollection.h"
#include "Registration/Registration.h"
#include "Commands/Command.h"
#include <memory>

namespace shen
{
    /*class DataElementWrapper;

    class InputCommandLoader
    {
    public:
        void LoadTypeAndId(const DataElementWrapper& elementWrapper, Command* command);

        virtual std::unique_ptr<Command> LoadCommand(const DataElementWrapper& elementWrapper) { return nullptr; }
    };*/
}
