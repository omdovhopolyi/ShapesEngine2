#pragma once

#include "InputCommandLoader.h"
#include "Serialization/DataElementWrapper.h"

namespace shen
{
    /*void InputCommandLoader::LoadTypeAndId(const DataElementWrapper& serialization, Command* command)
    {
        command->SetId(serialization.GetStr("id"));
        command->SetType(serialization.GetStr("type"));
        command->SetGlobal(serialization.GetBool("global"));
    }*/
}
