#include "PauseGameCommandLoader.h"
#include "Commands/PauseGameCommand.h"

namespace shen
{
    /*REGISTER_CLASS_LOADER(PauseGameCommandLoader)

    std::unique_ptr<Command> PauseGameCommandLoader::LoadCommand(const DataElementWrapper& serialization)
    {
        auto command = std::make_unique<PauseGameCommand>();
        LoadTypeAndId(serialization, command.get());
        return command;
    }*/
}
