#pragma once

namespace shen
{
    /*class SystemsManager;

    class BaseSystem
    {
    public:
        virtual ~BaseSystem() = default;

        virtual void Init(SystemsManager* systems);
        virtual void Start() {};
        virtual void Stop() {};

        std::type_index GetTypeIndex() { return _typeIndex; }
        std::string GetTypeName() { return _typeName; }

    protected:
        void SetTypeIndex(std::type_index typeIndex) { _typeIndex = typeIndex; }
        void SetTypeName(const std::string& typeName) { _typeName = typeName; }

    protected:
        SystemsManager* _systems = nullptr;
        std::type_index _typeIndex;
        std::string _typeName;
    };*/
}

